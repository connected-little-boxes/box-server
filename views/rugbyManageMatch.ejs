<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Connected Little Boxes</title>
</head>

<body>
    <div class="container mb-2 mt-2">
        <h1 class="mb-4">Robot Rugby</h1>
        <h2 class="mb-4">Match Manage <%= match.name %></h2>
    </div>
    <div class="container mb-4">
        <div class="container mb-4">
            <form action="/rugbyMatch/editMatch/<%= match.name %>" method="POST">
                <div>
                    <label for="description">Description:</label>
                    <input type="text" id="description" name="description" value="<%= match.description %>" required
                        class="form-control">
                </div>
                <input type="submit" value="Update Description" class="btn btn-primary mt-2 btn-block">
            </form>
        </div>
        <div class="container mb-4">
            <a href="/rugbyMatch/getMatchAssets/<%= match.name %>" class="btn btn-primary mb-4 btn-block">Get match
                assets</a>
        </div>
        <div class="container mb-4">
            <a href="/rugbyMatch/commandAllRobots/<%= match.name %>/RS" class="btn btn-primary mb-4 btn-block">Start all robots</a>
        </div>
        <div class="container mb-4">
            <a href="/rugbyMatch/commandAllRobots/<%= match.name %>/RH" class="btn btn-primary mb-4 btn-block">Stop all robots</a>
        </div>
        <div class="container mb-4">
            <a href="/rugbyMatch/sendProgramAndRun/<%= match.name %>" class="btn btn-primary mb-4 btn-block">Send program and run</a>
        </div>
        <div class="container mb-2 mt-2">
            <form method="POST" action="/rugbyMatch/saveProgramToAllRobots/<%=match.name%>">
              <textarea id="codeTextarea" name="codeTextarea" style="font-family:monospace;" class="w-100" rows="20"
                cols="120"><%= match.resetCode %></textarea>
              <input type="submit" value="Send to all robots" class="btn btn-primary mt-2 mb-2 btn-block">
            </form>
        </div>
    </div>

</body>

</html>