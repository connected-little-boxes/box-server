<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">`
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <title>Connected Little Boxes</title>
</head>

<body>
    <div class="container mb-4">
        <h1 class="mb-4">Connected Little Boxes</h1>
        <h2 class="mb-2">
            Edit Message
        </h2>
    </div>
    <div class="container mb-4">
        <div class="container mb-4">
            <form
                action="/command/commandMessageEdit/<%= commandGroup_id %>/<%= command_id %>/<%= commandMessage._id %>"
                method="POST">
                <div class="form-group">
                    <label for="name">Name</label>
                    <input type="text" required name="name" id="name" class="form-control" value="<%= commandMessage.name %>">
                    <label for="description">Description</label>
                    <input type="text" name="description" id="description" class="form-control"
                        value="<%= commandMessage.description %>">
                    <label for="deviceName">Device</label>
                    <select class="form-control" name="deviceName" id="deviceName">
                        <% deviceFriendlyNameList.forEach(name => { %>
                        <option value="<%=name %>"><%=name %></option>
                        <% }) %>
                      </select>                    
                    <label for="deviceName">Message</label>
                    <input required type="text" name="message" id="message" class="form-control"
                        value="<%= commandMessage.message %>">
                    <p class="mt-3 mb-3"> The message will be sent to the specified device, which is identified by its
                        friendly name. You can add a name and a
                        description of the message, but these are not compulsory.
                    </p>
                    <p>You can find a command description <a
                            href="https://github.com/connected-little-boxes/box-doc/blob/main/Connected%20Little%20Box%20Reference.pdf">here</a>.
                    </p>
                    <button type="submit" class="btn btn-primary mt-4 btn-block">Save</button>
            </form>
        </div>
</body>
</html>